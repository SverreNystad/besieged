package com.softwarearchitecture.game_server;

/**
 * Enum for the different types of tiles on the map.
 */
public enum MapTile {
    START,
    END,
    PATH,
    SURROUNDING,
    PLACABLE_LOCATION,
}
